<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.StoreMapper">

    <insert id="insertStore" parameterType="StoreDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO store (name, phone, ownerId, openStatus, categoryId, address, buildingManagementNum, createdAt)
        VALUES(#{name}, #{phone}, #{ownerId}, #{openStatus}, #{categoryId}, #{address}, #{buildingManagementNum}, #{createdAt})
    </insert>

    <select id="findByAddressCode" resultType="StoreDto">
        SELECT *
        FROM store
        JOIN store_delivery_location as sdl
        ON store.id = sdl.storeId
        WHERE sdl.addressCode = #{addressCode}
        AND sdl.status = "존재";
    </select>

</mapper>